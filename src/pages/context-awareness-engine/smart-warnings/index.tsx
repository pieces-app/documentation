import React from 'react';
import {Redirect} from '@docusaurus/router';

const Home = () => {
  return <Redirect to="/features/auto-enrichment#smart-warnings-and-sensitive-information-detection" />;
};

export default Home;
