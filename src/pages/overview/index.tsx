import React from 'react';
import {Redirect} from '@docusaurus/router';

const Home = () => {
  return <Redirect to="/learn/installation-getting-started/what-am-i-installing" />;
};

export default Home;
