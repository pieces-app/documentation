import React from 'react';
import {Redirect} from '@docusaurus/router';

const Home = () => {
  return <Redirect to="/docs/features/auto-enrichment#smart-warnings-and-sensitive-information-detection" />;
};

export default Home;
