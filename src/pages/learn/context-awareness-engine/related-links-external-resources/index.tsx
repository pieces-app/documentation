import React from 'react';
import {Redirect} from '@docusaurus/router';

const Home = () => {
  return <Redirect to="/learn/features/auto-enrichment#related-links-external-resources--reference-materials" />;
};

export default Home;
