# Dart-tutorial Snippet Collections

Here is the list of page links where ```"save to pieces"``` need to go. Each section is split up sequentially based on the:

- page
    - snippets
    - links

For each corresponding page. **As you approach a divider**, that indicates a new page beginning. **YouTube Power tip Banners** can be found at the end of each page list.

## Links Under Snippets

<Accordion defaultOpen title="Convert String to Int in Dart">

    [Convert String to Int](https://dart-tutorial.com/dart-how-to/convert-string-to-int-in-dart/)

    ```dart title="Convert String to Int in Dart"
    int.parse("String");
    ```

    [Sharable Link for Code](#)

    ```dart title="Example To Convert String to Int"
    void main(){
    String value = "10";
    int numericValue = int.parse(value);
    print("Type of value is ${value.runtimeType}");
    print("Type of numeric value is ${numericValue.runtimeType}");
    }
    ```

    [Sharable Link for Code](#)

    ```dart title="Example 2 To Convert String to Int"
    void main(){
    String value = "10";

    int num2 = 20;
    int num1 = int.parse(value);

    int sum = num1 + num2;

    print("Type sum is $sum");
    }
    ```

    [Sharable Link for Code](#)

    ```dart title="Things To Remember While Casting String To Int"
    void main(){
    String value = "10";

    int num2 = 20;
    int num1 = int.parse(value);

    int sum = num1 + num2;

    print("Type sum is $sum");
    }
    ```

    [Sharable Link for Code](#)

    ```shell title="Example With Try Catch"
    void main(){
    try {
    String value = "hello";
    int numericValue = int.parse(value);
    print("Type of numeric value is ${numericValue.runtimeType}");

    }
    catch(ex){
    print("Something went wrong.");
    }

    }
    ```

    [Sharable Link for Code](#)

</Accordion>










