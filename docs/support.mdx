---
title: Pieces for Developers | Support
description: Pieces for Developers Support
---

export const QuoteCallout = props => {
    return <div style={{padding: "5px 30px", borderRadius: "20px"}}>
        <p><strong>" </strong><i>{props.children}</i><strong> "</strong></p>
    </div>
}

export const InstallButtonWindows = props => {
    return <a style={{textDecoration: "none"}} href={ props.src }>
        <div style={{border: "1px solid white", backgroundColor: "black", maxWidth: "fit-content", minWidth: "fit-content", display: "flex", flexDirection: "row", alignItems: "center", maxHeight: "50px", borderRadius: "40px", padding: "4px 20px", marginRight: "8px", marginBottom: "16px"}}>
            { props.children }
            <p style={{backgroundColor: "black", color: "white", marginLeft: "8px"}}>Get Pieces for Windows</p>
        </div>
    </a>
}

export const InstallButtonMac = props => {
    return <a style={{textDecoration: "none"}} href={ props.src }>
        <div style={{border: "1px solid white", backgroundColor: "black", maxWidth: "fit-content", minWidth: "fit-content", display: "flex", flexDirection: "row", alignItems: "center", maxHeight: "50px", borderRadius: "40px", padding: "4px 20px", marginRight: "8px", marginBottom: "16px"}}>
            { props.children }
            <p style={{backgroundColor: "black", color: "white", marginLeft: "8px"}}>Get Pieces for macOS</p>
        </div>
    </a>
};

export const InstallButtonLinux = props => {
    return <a style={{textDecoration: "none"}} href={ props.src }>
        <div style={{border: "1px solid white", backgroundColor: "black", maxWidth: "fit-content", minWidth: "fit-content", display: "flex", flexDirection: "row", alignItems: "center", maxHeight: "50px", borderRadius: "40px", padding: "4px 20px", marginRight: "8px", marginBottom: "16px" }}>
            { props.children }
            <p style={{color: "white", marginLeft: "8px"}}>See for Linux</p>
        </div>
    </a>
}

export const ReleasePill = props => {
    return <a target="_blank" style={{textDecoration: "none"}}
              href={props.src}>
        <div style={{maxWidth: "fit-content", minWidth: "fit-content"}}>
            <p class="dark:text-white text-black"
               style={{
                   padding: "8px 20px",
                   borderRadius: "8px",
                   border: "1px solid #f3f5f6",
                   fontWeight: "500",
                   background: "transparent",
                   marginBottom: "8px",
                   marginTop: "3px",
                   marginRight: "16px"
               }}>{props.children}</p>
        </div>
    </a>
}

export const JumpButtons = props => {
    return <a target="_blank" style={{textDecoration: "none", marginRight: "18px"}} href={props.src}>
        <div style={{maxWidth: "275px", minWidth: "150px", textAlign: "center"}}>
            <p class="dark:text-white text-black"
               style={{
                   padding: "8px 20px",
                   borderRadius: "8px",
                   border: "1px solid #f3f5f6",
                   backgroundColor: "transparent",
                   fontWeight: "500",
                   marginBottom: "8px",
                   marginTop: "3px"
               }}>{props.children}</p>
        </div>
    </a>
}

export const PillsFlexColumn = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'column',
        alignContent: 'start',
        flexWrap: "wrap"
    }}>{props.children}</div>;
};

export const GridFlexRow = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'start',
        flexWrap: "wrap"
    }}>{props.children}</div>;
};

export const GridFlexSpaceEvenlyRow = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-evenly',
        flexWrap: "wrap"
    }}>{props.children}</div>;
};

export const Spacer = props => {
    return <div style={{height: "16px"}}></div>
}

export const HeaderImage = props => {
    return <picture>
        <source srcSet={props.srcset} media="(prefers-color-scheme: dark)"/>
        <img src={props.src}/>
    </picture>
}

export const SingleCard = props => {
  return (
    <div style={{
      width: '100%',
      boxSizing: 'border-box',
      padding: '10px',
      margin: '0 auto',
      backgroundColor: '#f8f9fa',
      borderRadius: '5px',
      boxShadow: '0 2px 5px rgba(0, 0, 0, 0.15)'
    }}>
      {props.children}
    </div>
  );
};

# Support Center

<SingleCard>
  GitHub Issues
</SingleCard>

<SingleCard>
  Form
</SingleCard>
